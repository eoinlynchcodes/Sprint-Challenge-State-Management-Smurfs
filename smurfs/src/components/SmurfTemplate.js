import React from 'react';

export function SmurfTemplate(props){

    console.log(props.item);

    return (
        <div>
            {/* <h1>{props.item.name}</h1>
            <h1>{props.item.age}</h1>
            <h1>{props.item.height}</h1>
            <h1>{props.item.id}</h1> */}
        </div>
    )
}